{
  "name": "js-catalog",
  "version": "1.0.0",
  "description": "无依赖生成文档目录",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel --presets es2015 src/index.js -o dist/js-catalog.js && uglifyjs dist/js-catalog.js -m -o dist/js-catalog.min.js"
  },
  "keywords": [
    "js",
    "js-catalog",
    "catalog"
  ],
  "author": "scyuan",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "uglify-js": "^3.4.9"
  }
}